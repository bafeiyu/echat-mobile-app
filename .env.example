# EChat 小部件 websiteToken；用于在设置页打开内嵌 EChat 小部件。
# 打包是否必须：否（缺省不显示小部件）。
# 获取方式：在 EChat 后台「设置 > Inboxes > Website」中复制 website token。
EXPO_PUBLIC_EChat_WEBSITE_TOKEN=

# EChat 服务器基础地址，用于小部件及默认接口域名。
# 打包是否必须：建议填；未填会使用 https://app.EChat.com。
# 获取方式：自建或托管 EChat 实例的访问域名，如 https://chat.example.com。
EXPO_PUBLIC_EChat_BASE_URL=https://app.echat.com

# June Analytics SDK Key，用于生产环境埋点上报。
# 打包是否必须：否（留空则禁用埋点）。
# 获取方式：在 June 项目设置里复制 Server/SDK Key（Basic Auth）。
EXPO_PUBLIC_JUNE_SDK_KEY=

# 允许的最低 EChat 服务端版本，低于此版本会提醒升级。
# 打包是否必须：否（不填则不提示版本升级），建议保持与后端兼容要求一致。
# 获取方式：根据后端部署版本填写，如 4.1.0。
EXPO_PUBLIC_MINIMUM_EChat_VERSION=4.1.0

# Sentry DSN，用于采集线上崩溃与性能数据。
# 打包是否必须：否（不填则不初始化 Sentry）。
# 获取方式：在 Sentry 项目 Settings > Client Keys 里复制 DSN。
EXPO_PUBLIC_SENTRY_DSN=

# Expo/EAS 项目 ID，用于 EAS Build 与 OTA 更新关联项目。
# 打包是否必须：是（使用 EAS 构建时需要）。
# https://expo.dev/
# 获取方式：在 Expo Dashboard 项目设置中复制 Project ID，或运行 `eas init` 后生成。
EXPO_PUBLIC_PROJECT_ID=

# Expo slug（应用标识），影响包名与 OTA 通道。
# 打包是否必须：建议填（不填默认 e-chat-mobile）。
# 获取方式：自定义设置，例如 e-chat-mobile。
EXPO_PUBLIC_APP_SLUG=e-chat-mobile

# Sentry 项目名称，启用 Expo Sentry 插件时需要（需与组织同时配置）。
# 打包是否必须：否（未填写则跳过插件）。
# 获取方式：Sentry 项目 Settings > General 中的 Project Slug。
EXPO_PUBLIC_SENTRY_PROJECT_NAME=

# Sentry 组织名称，启用 Expo Sentry 插件时需要。
# 打包是否必须：否（未填写则跳过插件）。
# 获取方式：Sentry 组织 Settings 中的 Organization Slug。
EXPO_PUBLIC_SENTRY_ORG_NAME=

# iOS Firebase 配置文件路径（相对路径），如 ./GoogleService-Info.plist。
# 打包是否必须：建议 仅在启用 Firebase 推送/分析时需要。
#  https://console.firebase.google.com/
# 获取方式：在 Firebase 控制台下载 iOS 应用的 GoogleService-Info.plist。
EXPO_PUBLIC_IOS_GOOGLE_SERVICES_FILE=

# Android Firebase 配置文件路径（相对路径），如 ./google-services.json。
# 打包是否必须：仅在启用 Firebase 推送/分析时需要。
#  https://console.firebase.google.com/
# 获取方式：在 Firebase 控制台下载 Android 应用的 google-services.json。
EXPO_PUBLIC_ANDROID_GOOGLE_SERVICES_FILE=

# Apple 开发者账号 ID（邮箱），供 EAS iOS 构建/签名使用。
# 打包是否必须：仅 iOS 发行版需要（用于自动登录或提交）。
# 获取方式：Apple Developer 账户邮箱。
EXPO_APPLE_ID=

# Apple Team ID，对应 Apple Developer 团队标识。
# 打包是否必须：仅 iOS 发行版需要。
# 获取方式：登录 Apple Developer 或 App Store Connect，在 Membership 信息查看 Team ID。
EXPO_APPLE_TEAM_ID=

# 是否启用 Storybook（'true'/'false'），用于组件调试预览。
# 打包是否必须：否，默认 false。
# 获取方式：自定义配置。
EXPO_STORYBOOK_ENABLED=false
